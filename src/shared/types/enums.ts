export enum DatabasePaths {
  USERS = 'users',
  ADMIN = 'admin',
}
export enum Direction {
  DESC = 'desc',
  ASC = 'asc',
}
export enum UserDataEnum {
  ID = 'id',
  NAME = 'name',
  RULE = 'rule',
  NICKNAME = 'nickName',
  PHONE = 'phone',
  EMAIL = 'email',
  BALANS = 'balans',
}
